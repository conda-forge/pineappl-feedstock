{% set name = "pineappl" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/N3PDF/pineappl/archive/v{{ version }}.tar.gz
  sha256: a0c6b195c795ce8487d1d4a0c440031bf552ff58057a2a3476c3b7c10b40c589

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler("c") }}
  host:
    - rust >=1.45
    - pkg-config
    - openssl
    - python
    - pip
  run:
    - python
    - numpy
    - pkgconfig

test:
  imports:
    - pineappl
  requires:
    - pytest
  commands:
    - pytest --pyargs pineappl

about:
  home: https://github.com/N3PDF/pineappl
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  summary: PineAPPL is not an extension of APPLgrid.

extra:
  recipe-maintainers:
    - scarrazza
    - cschwan
