schema_version: 1

context:
  name: pineappl
  version: "1.0.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/NNPDF/pineappl/archive/v${{ version }}.tar.gz
  sha256: fc06d88318a0029071e15370a5a62f8e7749f801c97d7fbec63cb1b99f4d4f0f

build:
  number: 0
  skip:
    - pypy
    - win

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - pkg-config
    - maturin
    - ${{ compiler('rust') }} >=1.70
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
  host:
    - ${{ compiler('c') }}
    - pkg-config
    - lhapdf
    - python
    - pip
    - maturin
  run:
    - pkg-config
    - lhapdf
    - python
    - numpy

tests:
  - python:
      imports:
        - pineappl

about:
  license: GPL-3.0-only
  license_file: LICENSE
  summary: PineAPPL is not an extension of APPLgrid.
  homepage: https://github.com/NNPDF/pineappl

extra:
  recipe-maintainers:
    - Radonirinaunimi
    - scarlehoff
    - scarrazza
    - cschwan
